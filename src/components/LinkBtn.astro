---
const { href, additionalClasses = '' } = Astro.props;
const baseClasses = "btn btn-ghost btn-sm text-sm mb-10";
const classes = `${baseClasses} ${additionalClasses}`;
---
<a {href} class={classes}>
    <slot />
</a>
